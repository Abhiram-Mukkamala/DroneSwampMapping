/**
 * Terrain & Hazard Data Module — Phase 2+
 *
 * Will contain: terrain heightmap, obstacle grid,
 * no-fly zones, hazard regions.
 *
 * @returns {object|null} — terrain grid data, null in Phase 1
 */
export function getTerrainData() {
  return null;
}

/**
 * @returns {Array<object>} — obstacles [{position, radius, height, shape}]
 */
export function getObstacles() {
  return [];
}
