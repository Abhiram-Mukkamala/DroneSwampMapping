import asyncioimport websocketsimport jsonfrom data_pipeline import PipelineNodefrom obstacle_map import ObstacleMappipeline = PipelineNode(grid_width=100, grid_height=100, cell_size=5)obs_map = ObstacleMap()async def master_router(websocket):    async for message in websocket:        packet = json.loads(message)                if packet.get("type") == "yolo_detections":            hazard_map = pipeline.process_yolo_detections(packet["payload"])            obs_map.update_dynamic_grid(hazard_map["grid"], hazard_map["cellSize"])                        repulsion_forces = {}            for d_id, state in pipeline.telemetry_stream.items():                pos = [state["position"]["x"], state["position"]["y"], state["position"]["z"]]                force = obs_map.calculate_repulsion(pos)                repulsion_forces[d_id] = force.tolist()                            hazard_map["repulsion"] = repulsion_forces                        await websocket.send(json.dumps({"type": "hazard_map", "payload": hazard_map}))                    elif packet.get("type") == "telemetry":            global_state = pipeline.process_drone_telemetry(packet["payload"])            await websocket.send(json.dumps({"type": "global_telemetry", "payload": global_state}))async def main():    async with websockets.serve(master_router, "localhost", 8765):        print("=É‹« Master Simulation Mainframe LIVE on ws://localhost:8765")        await asyncio.Future()if __name__ == "__main__":    asyncio.run(main())